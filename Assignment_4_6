def computepay(h,r):
    if h >= 40:
    	return(float(40*float(r) + (h-40)*float(r)*1.5))
    if h < 40:
    	return(float(h * r))

hours = input("Enter Hours:")
rate = input("Enter Rate:")
h = float(hours)
r = float(rate)
print("Pay", computepay(h,r))
